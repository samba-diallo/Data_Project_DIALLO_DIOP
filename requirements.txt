# Packages additionnels requis (construire manuellement - NE PAS utiliser pip freeze)
# Installer avec : python -m pip install -r requirements.txt

# Web framework
dash>=2.5,<3.0
# dash-bootstrap-components : grille responsive, cards, NavbarSimple, Container...
# Recommandé par les skills frontend-design et ui-ux-pro-max
dash-bootstrap-components>=1.5,<2.0
# dash-iconify : accès aux icônes SVG (Heroicons, Lucide, Tabler...) sans emojis
# Conforme à la règle "no-emoji-icons" de la checklist UI/UX
dash-iconify>=0.1,<1.0
plotly>=5.0,<6.0

# Data processing (pandas 2.0+ for Python 3.9, pandas 2.2+ for Python 3.10+)
pandas>=1.5,<3.0.0; python_version < '3.10'
pandas>=2.0,<3.0.0; python_version >= '3.10'
# openpyxl : lecture des fichiers Excel (.xlsx) source ADEME
openpyxl>=3.1,<4.0

# Testing
pytest>=7.0,<8.0

# Production server
gunicorn>=20.1,<21.0

